<Window x:Class="WpfDemo2.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:model="clr-namespace:WpfDemo2.Core.Models"
        xmlns:command="clr-namespace:WpfDemo2.Core.Command"
        xmlns:helper="clr-namespace:WpfDemo2.Core.Helper"
        mc:Ignorable="d"
        Title="登录" Width="1000" Height="560" WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="2.5*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0">
            <Image Source="/Assets/Images/login_bg.png" Stretch="UniformToFill"/>
        </Border>

        <Border Grid.Column="1" Background="White">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="40,40" HorizontalAlignment="Center" MaxWidth="320">
                    <TextBlock Text="登录" FontSize="28" FontWeight="Bold" Foreground="#333" Margin="0,0,0,10"/>
                    <TextBlock Text="欢迎登录视频超融合AI平台" FontSize="14" Foreground="#888" Margin="0,0,0,20"/>

                    <StackPanel Margin="0,0,0,12">
                        <TextBlock Text="手机号" Foreground="#666" Margin="0,0,0,6"/>
                        <TextBox Height="36" Padding="8,4" VerticalContentAlignment="Center" Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>

                    <StackPanel Margin="0,0,0,12">
                        <TextBlock Text="密码" Foreground="#666" Margin="0,0,0,6"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.Resources>
                                <BooleanToVisibilityConverter x:Key="BoolToVis"/>
                            </Grid.Resources>
                            <TextBox Height="36" Width="280" Padding="8,4" VerticalContentAlignment="Center" Text="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Visibility="{Binding ShowPassword, Converter={StaticResource BoolToVis}}"/>
                            <PasswordBox Height="36" Width="280" Padding="8,4" helper:PasswordBoxHelper.BindPassword="True" helper:PasswordBoxHelper.BoundPassword="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                                <PasswordBox.Style>
                                    <Style TargetType="PasswordBox">
                                        <Setter Property="Visibility" Value="Visible"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ShowPassword}" Value="True">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </PasswordBox.Style>
                            </PasswordBox>
                            <Button Grid.Column="1" Width="60" Height="36" Margin="8,0,0,0" Command="{Binding TogglePasswordVisibilityCommand}">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Content" Value="显示"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ShowPassword}" Value="True">
                                                <Setter Property="Content" Value="隐藏"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </Grid>
                    </StackPanel>

                    <StackPanel Margin="0,0,0,12">
                        <TextBlock Text="验证码" Foreground="#666" Margin="0,0,0,6"/>
                        <DockPanel LastChildFill="False">
                            <TextBox Height="36" Padding="8,4" VerticalContentAlignment="Center" DockPanel.Dock="Left" Width="150" Text="{Binding CaptchaInput, UpdateSourceTrigger=PropertyChanged}" />
                            <Border DockPanel.Dock="Right" Width="70" Height="36" Background="#eee" BorderBrush="#ddd" BorderThickness="1" HorizontalAlignment="Right" VerticalAlignment="Center">
                                <TextBlock Text="{Binding CaptchaCode}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16" FontWeight="Bold"/>
                            </Border>
                            <Button Content="刷新" DockPanel.Dock="Right" Width="60" Height="36" Margin="8,0,0,0" Command="{Binding RefreshCaptchaCommand}"/>
                        </DockPanel>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,16">
                        <CheckBox Content="记住密码" IsChecked="{Binding RememberMe}" />
                        <TextBlock Text="忘记密码" Foreground="#409EFF" Margin="20,0,0,0">
                            <TextBlock.InputBindings>
                                <MouseBinding Command="{Binding ForgotPasswordCommand}" MouseAction="LeftClick"/>
                            </TextBlock.InputBindings>
                        </TextBlock>
                    </StackPanel>

                    <Button Content="登 录" Height="38" Width="280" Background="#409EFF" Foreground="White" FontSize="16" Command="{Binding LoginCommand}"/>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</Window>